export interface reserveDTO {
    id_renter: number,
    event_name: string,
    start_date: string,
    final_date: string,
    total_amount: number,
    received_amount: number,
    balance: number,
    observation: string,
    create_by: number,
}