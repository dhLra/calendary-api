export interface updateReservesDTO {
    id_reserve: number,
    event_name: string,
    start_date: string,
    final_date: string,
    total_amount: number,
    received_amount: number,
    balance: number,
    observation: string,
}